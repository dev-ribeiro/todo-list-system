{
  "name": "xneo/app",
  "type": "project",
  "license": "MIT",
  "autoload": {
    "psr-4": {
      "App\\": "app/"
    }
  },
  "require": {
    "robmorgan/phinx": "^0.13.4"
  },
  "scripts": {
    "start:dev": "php -S localhost:8000 -d PHP_ENV=dev",
    "start:prod": "php -S localhost:8000 -d PHP_ENV=prod",
    "migrate:dev": "vendor/bin/phinx migrate --configuration=phinx.php",
    "migrate:prod": "vendor/bin/phinx migrate --configuration=phinx.production.php",
    "load": "composer dump-autoload -o"
  },
  "config": {
    "process-timeout": 0
  }
}
